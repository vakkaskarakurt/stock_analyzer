{
  "scripts": {
    "start": "npx concurrently -k --names \"BACKEND,FRONTEND,PREDICTION\" --prefix-colors \"blue,magenta,yellow\" \"cd Backend/StockAnalyzer.Api && dotnet run\" \"cd Frontend && npm start\" \"cd Prediction && python -m uvicorn main:app --reload --port 8000\""
  },
  "dependencies": {
    "playwright": "^1.58.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
